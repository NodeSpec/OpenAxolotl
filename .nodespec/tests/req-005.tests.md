# Test Plan: REQ-005 - Camera System

## Testing Objectives

**Requirement:** Camera System
**Category:** functional
**Description:** A third-person camera that reads well across both movement grammars and inside confined spaces. Must handle full 3D underwater freedom (where the axolotl can pitch and roll) and grounded land traversal (where a more conventional follow camera reads better) without the player fighting it. Supports per-world camera hint volumes so a Level Contract-conforming world can direct framing for set pieces, tunnels, and boss arenas without scripting its own camera. Collision-aware so tight underwater tunnels and cave interiors never clip through geometry.

## Project Context

OpenAxolotl — Play it. Fork it. Build a world.

A colorful 3D action-platformer in Godot where a tiny axolotl explores strange aquatic worlds, restoring damaged habitats and rescuing creatures. Every world is a self-contained module anyone — human or AI coding agent — can fork, replace, or extend. Open source is a gameplay feature, not a line in the README.

FOUR PILLARS

1. Regeneration is the co …

## Acceptance Criteria

Criterion text lives HERE once; every section below cites the AC id.

- **AC-REQ-005-1** [automated] [VERIFIED] Camera follows the axolotl in both water and land grammars, and during a grammar transition its per-frame position and rotation deltas stay at or below tuning keys camera.smoothing.max_position_delta_m_per_frame and camera.smoothing.max_rotation_delta_deg_per_frame
- **AC-REQ-005-2** [automated] [VERIFIED] Camera performs collision avoidance against level geometry, never clipping inside walls or terrain in confined tunnels
- **AC-REQ-005-3** [automated] [VERIFIED] Worlds can place camera hint volumes that override framing (distance, angle, lock axis) declaratively without custom camera code
- **AC-REQ-005-4** [automated] [VERIFIED] Camera behavior is fully driven by exported configuration values rather than hardcoded constants
- **AC-REQ-005-5** [manual] [PENDING] Camera never induces disorientation or fights the player during underwater traversal in hands-on play

## Criterion Assessment

2 of 5 criteria have potential quality issues:

**AC-REQ-005-2:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form

**AC-REQ-005-4:**
- Warning: No clear trigger or action verb -- hard to derive a test case
- Suggestion: Rewrite in 'When [trigger], [system] shall [observable outcome]' form


## Recommended Test Types

### Acceptance / BDD
- **Scope:** Requirement-level behavior validation
- **Rationale:** 5 acceptance criteria defined

### Unit
- **Scope:** Individual function/method correctness
- **Rationale:** 9 source artifact(s) available for targeted testing

## Suggested Framework

**Framework:** GdUnit4
**Reason:** Godot 4's maintained test framework (GDScript and C#); GUT is the Godot 3 answer

## Architecture Components Under Test

| Component | Role | Technology |
|-----------|------|------------|
| Camera System | shared-library | godot |

## Interfaces & Contracts to Verify

- **OpenAxolotl Game Client** -> **Camera System** via `dependency` ("Core Module Dependency") — dependency contract (no payload schema expected)
- **World: Coral Cove** -> **Camera System** via `dependency` ("Camera Hint Interface") — dependency contract (no payload schema expected)
- **World: Bubble Bay** -> **Camera System** via `dependency` ("Camera Hint Interface") — dependency contract (no payload schema expected)
- **Camera System** -> **Balance and Tuning Data** via `dependency` ("Tuning Data Interface") — dependency contract (no payload schema expected)
- **World Static Analysis Gate** -> **Camera System** via `dependency` ("Engine Feature Policy") — dependency contract (no payload schema expected)

## Source Artifacts

| File | Kind | Language | Component |
|------|------|----------|-----------|
| `core/camera/camera_occluder_probe.gd` | source | --- | Camera System |
| `core/camera/scene_occluder_probe.gd` | source | --- | Camera System |
| `core/camera/camera_hint_volume.gd` | source | --- | Camera System |
| `core/camera/camera_rig.gd` | source | --- | Camera System |
| `test/core/camera/test_camera_rig.gd` | source | --- | Camera System |
| `core/camera/camera_framing.gd` | source | --- | Camera System |
| `core/camera/camera_hint.gd` | source | --- | Camera System |
| `core/camera/camera_hint_stack.gd` | source | --- | Camera System |
| `core/camera/camera_follow.gd` | source | --- | Camera System |

## Automated Test Scenarios

Derived section — regenerates when criteria, mappings, or contract schemas change; do not edit. Per scenario: derive the test from the cited criterion and the contract schemas, implement and run it, then report the outcome via report_test_results using the suggested test_id and the criterion's EXACT text as criterion_text (that binding is what flips met).

#### AC-REQ-005-1: camera-follows-the-axolotl-in-both-water-and-land-grammars-a
Derive the test from AC-REQ-005-1 and report the outcome with test_id "TC-REQ-005-1".

#### AC-REQ-005-2: camera-performs-collision-avoidance-against-level-geometry-n
Derive the test from AC-REQ-005-2 and report the outcome with test_id "TC-REQ-005-2".

#### AC-REQ-005-3: worlds-can-place-camera-hint-volumes-that-override-framing-d
Derive the test from AC-REQ-005-3 and report the outcome with test_id "TC-REQ-005-3".

#### AC-REQ-005-4: camera-behavior-is-fully-driven-by-exported-configuration-va
Derive the test from AC-REQ-005-4 and report the outcome with test_id "TC-REQ-005-4".

## Contract Validation

Derived checks per contract — regenerates when schemas change; do not edit.

#### Core Module Dependency (outbound: Camera System)
- [ ] should verify Core Module Dependency contract between OpenAxolotl Game Client and Camera System
- [ ] should handle Camera System unavailability gracefully

#### Camera Hint Interface (outbound: Camera System)
- [ ] should verify Camera Hint Interface contract between World: Coral Cove and Camera System
- [ ] should handle Camera System unavailability gracefully

#### Camera Hint Interface (outbound: Camera System)
- [ ] should verify Camera Hint Interface contract between World: Bubble Bay and Camera System
- [ ] should handle Camera System unavailability gracefully

#### Tuning Data Interface (outbound: Balance and Tuning Data)
- [ ] should verify Tuning Data Interface contract between Camera System and Balance and Tuning Data
- [ ] should handle Balance and Tuning Data unavailability gracefully

#### Engine Feature Policy (outbound: Camera System)
- [ ] should verify Engine Feature Policy contract between World Static Analysis Gate and Camera System
- [ ] should handle Camera System unavailability gracefully

## Manual Verification

These criteria are proven by a HUMAN: perform the check, tick the criterion box in the owning node's task document, and have the user approve the resulting change card — that approval is what flips met. report_test_results REFUSES to bind manual criteria; do not author test cases for them.

- [ ] AC-REQ-005-5 — verify per the criterion text above, then tick + approve via the task doc

## Test Strategy

<!-- Edit this section to refine the testing approach -->

### Setup & Fixtures

- [ ] Define test data fixtures
- [ ] Set up mock services for external dependencies
- [ ] Configure test environment variables
