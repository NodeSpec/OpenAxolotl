# Makes tools/ importable as a package so pyproject.toml can expose the
# validators as console commands (oax-level-check, oax-static-gate, oax-test).
# The modules themselves stay runnable directly and standard-library-only.
